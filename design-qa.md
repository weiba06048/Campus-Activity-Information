# 设计 QA 记录（第四轮 · 校园论坛）

- 源视觉稿路径：无。本会话没有可用的图像生成工具，Product Design 的 `ideate`（生成 3 个视觉方案）步骤无法执行，因此没有源稿可做逐像素比对。
- 实现截图路径：`.preview/home.png`、`.preview/category.png`、`.preview/activity.png`、`.preview/activity-trust.png`、`.preview/search-panel.png`、`.preview/narrow-home.png`、`.preview/narrow-detail.png`
- 视口：桌面 1440px 宽（高度 620 - 1400px 按内容设定）；窄屏 390px 宽（1300 - 1400px）
- 像素尺寸与密度归一化：截图由 Edge CDP `Emulation.setDeviceMetricsOverride` 固定 `deviceScaleFactor=1`、`mobile` 按宽度切换，输出尺寸与设定视口一致（1440x1300、1440x1000、1440x1400、1440x1200、1440x620、390x1400、390x1300）。
- 状态：首页；板块页（竞赛与挑战）；活动详情（蓝桥杯训练营，含「最新通知」）；活动详情（周末羽毛球约球，含「学生自发」标识）；搜索下拉（关键词「科研」）；窄屏首页；窄屏活动详情（Git 与 GitHub 零基础工作坊）；不存在的活动 id（空状态）。

## 全屏对比证据

无源视觉稿，无法执行源稿与实现的并排比对。改为按设计意图逐项核对：排版与层级、间距与栅格节奏、颜色与状态色、图标一致性、图片资产（本设计不含位图资产，不涉及）、文案准确性、交互状态、响应式。

## 局部区域对比证据

未做。无源稿，局部裁剪对比缺少基准。

## Findings

- [P1] 首页「最近值得去」把已结束的活动排在最前
  Location: `src/App.jsx` -> `HomePage`
  Evidence: 按开始时间排序后，9 月 18 日已结束的分享会占据首卡位，三张推荐卡里两张状态为「已结束 / 即将开始」混排。
  Impact: 推荐位是首页最强的行动入口，展示已结束活动会让用户白点一次。
  Fix: 推荐位过滤掉 `已结束` 状态后再取前三。已修复，修复后首屏三张卡均为今天可参加的活动。

- [P1] 关键字段缺失时列表与详情出现「信息未注明」堆叠
  Location: `src/App.jsx` -> `ActivityCard`、`InfoRow`、搜索下拉、`ActivityPage` 报名区
  Evidence: 真实数据中有约十场活动未提供地点、报名方式与联系方式，卡片地点行与详情页「怎么报名」直接显示两行「信息未注明」；报名截止行还会套用琥珀色强调。
  Impact: 页面显得像半成品，且琥珀色强调会让人误以为「信息未注明」是需要立刻行动的截止信息。
  Fix: 卡片地点改为浅灰「地点未注明」；详情页报名/联系方式在缺失时改为一句可执行的提示（「报名方式未注明，建议向活动方或所在学院确认」）；缺失值不再套用强调色；搜索下拉在无地点时改显示日期信息。已修复。

- [P2] 页脚仍在说明「示例数据」
  Location: `src/App.jsx` -> `Footer`
  Evidence: 页脚写着「当前展示的是示例数据，替换 src/data/activities.js 即可…」，与真实数据版本不符。
  Impact: 会让访问者怀疑活动信息是否真实。
  Fix: 改为「活动信息整理自各活动方发布内容，时间、地点如有调整以活动方最新通知为准；学生自发活动请自行核实后再参加」。已修复。

- [P2] 首页统计文案与实际状态不符
  Location: `src/App.jsx` -> `HomePage` 副标题
  Evidence: 原文案称「22 场活动正在进行或即将开始」，但其中包含 1 场已结束活动。
  Impact: 数字口径不准确。
  Fix: 改为「共 6 个活动板块、22 场活动，其中 N 场正在报名」。已修复。

- [P3] 同板块推荐行的日期列偏窄导致换行
  Location: `src/styles.css` -> `.related-date`
  Evidence: 「9 月 21 日 18:00 校内意向登记截止」在 130px 列内折成两行。
  Impact: 轻微观感问题。
  Fix: 列宽调整为 150px。已修复。

## 迭代历史

- 第 1 轮（首版示例数据）：完成桌面与窄屏截图自检，修复 favicon 404、详情段落间距、窄屏提示拥挤三项；控制台复检无报错。
- 第 2 轮（替换为真实活动数据，14:45）：重写 `src/data/activities.js`（6 个板块 / 22 场活动），同步扩展组件以支持 `dateKind`（活动日期 / 报名截止 / 长期开放三种日期块形态）、`trust`（学生自发标识）与「最新通知」区块。
- 第 2 轮首次截图发现：推荐位混入已结束活动、缺失字段堆叠、页脚文案过期、统计口径不准、推荐行日期列折行。修复后重新截取全部 7 张截图并复检。
- 第 2 轮复核：遍历 `#/`、`#/c/competition`、`#/c/workshop`、`#/a/lanqiao-camp`、`#/a/weekend-badminton`、`#/a/python-materials`、`#/search?q=科研`、`#/a/not-exist` 共 8 条路由，控制台无报错、无警告；搜索下拉实测输入「科研」返回 5 条结果并正确高亮。
- 构建验证：`npm run build` 成功（`dist/client`、`dist/server/index.js`、`dist/.openai/hosting.json`）；`npm run test:sites` 4 项全部通过。
- 第 3 轮（持久化 / 发布 / 评论，15:10）：新增 `src/storage.js`（localStorage 读写与时间格式化）、`src/store.jsx`（`DataProvider` + `useData()`）、`src/PublishPage.jsx`（发布与编辑）、`src/Comments.jsx`（讨论区），路由增加 `#/publish`。
- 第 3 轮首次截图发现：发布后的内容在首页与板块列表里都找不到（P1，见第三轮 Findings），已补「我发布的」板块。
- 第 3 轮复核：清空存储后走完「静态活动评论 → 刷新 → 发布活动 → 刷新 → 重新打开链接 → 搜索 → 编辑 → 删除」全流程，评论与发布内容在刷新与重开后都保留，控制台无报错；再次运行 `npm run build` 与 `npm run test:sites` 均通过。
- 第 4 轮（校园论坛，15:40）：新增 `src/Forum.jsx`（发帖 / 列表 / 详情 / 回复）、`src/store.jsx` 的论坛状态与动作、`src/storage.js` 的论坛读写，路由增加 `#/forum` 与 `#/forum/t/<帖子id>`，顶栏在「发布活动」右侧增加「论坛」按钮。
- 第 4 轮首次截图发现：窄屏下「论坛」按钮被挤到单独一行（P2），已调整窄屏顶栏布局并重新截图确认。
- 第 4 轮复核：空论坛 → 发帖 → 回复 → 刷新 → 列表排序 → 分类筛选 → 窄屏全流程走查通过，控制台无报错；`npm run build` 与 `npm run test:sites` 通过。

## 第三轮 · 本地持久化 / 发布活动 / 评论（2026-09-19）

- 新增截图：`.preview/publish-empty.png`、`.preview/publish-filled.png`、`.preview/posted-detail.png`、`.preview/comment-list.png`、`.preview/comment-added.png`、`.preview/after-reload.png`、`.preview/board-after-publish.png`、`.preview/home-with-post.png`、`.preview/verify-comment.png`、`.preview/verify-posted.png`、`.preview/verify-home-mine.png`
- 视口：桌面 1440px 宽（高度 1200 - 1500px 按内容设定），`deviceScaleFactor=1`
- 状态：空的发布表单；填好的发布表单；刚发布后的活动详情（含「我发布的」条、编辑 / 删除按钮）；活动详情的讨论区；发表评论后的讨论区；刷新后的同一详情页；发布后板块列表；首页含用户发布内容；重新打开链接后的详情页。

### Findings

- [P1] 发布后的内容缺少再次找回的入口
  Location: `src/App.jsx` -> `HomePage`
  Evidence: 走查中发布「周末桌游局（新手友好）」后刷新首页，正文里搜不到这条内容——首页「最近值得去」按开始时间取前三，新发布的活动排在后面；板块列表也按时间排序，用户发布的内容沉在末尾。
  Impact: 持久化本身没问题，但「刷新后还在」只有在用户找得到它时才有意义；发完关掉页面就基本只能靠搜索回忆标题。
  Fix: 首页新增「我发布的」板块（仅在有用户发布内容时渲染），列出自己发布的活动及其板块、日期、状态与评论数，点击进入详情页编辑或删除；`.my-post-list` / `.my-post` 样式与既有卡片体系保持一致。已修复。

- [P2] 详情页「报名截止」行出现「截止…截止」重复措辞
  Location: `src/App.jsx` -> `InfoRow`、发布表单「报名截止」字段
  Evidence: 用户把报名截止填成「9 月 25 日 20:00 截止」，详情页渲染为「报名截止 | 9 月 25 日 20:00 截止」。字段标签本身已含「截止」语义。
  Impact: 文案重复，观感略啰嗦；属于用户自由填写带来的可控问题，不是渲染缺陷。
  Fix: 未改动渲染逻辑（避免擅自裁剪用户填写的内容）；走查时按「9 月 25 日 20:00」填写，展示为「报名截止 | 9 月 25 日 20:00」。保留该字段的用户自由输入。

### 复核证据（Edge CDP 实测，非人工目测）

- 清空 `localStorage` 后首页回到「22 场活动」，三个存储键在首次渲染后即建立，说明读取失败时能安全退回默认值。
- 静态活动（`#/a/ai-intro-open-class`）的讨论区发表评论 → 刷新页面，评论内容与昵称「小红」都还在。
- 发布表单填写并提交 → 跳转 `#/a/user-…`，详情页显示「我发布的」标识；刷新、以及重新输入该链接打开，内容都还在。
- 发布后首页统计从 22 场变为 23 场，新活动出现在对应板块列表中，站内搜索「桌游」能命中，评论数会跟随显示在卡片上。
- 编辑页正确回填已发布内容，删除后回到首页且该条从 localStorage 中移除，同时静态活动的评论不受影响。
- 全程控制台无报错、无警告。

## 第四轮 · 校园论坛（2026-09-19）

- 新增截图：`.preview/forum-empty.png`、`.preview/forum-compose.png`、`.preview/forum-list.png`、`.preview/forum-list-two.png`、`.preview/forum-thread.png`、`.preview/forum-narrow.png`、`.preview/forum-narrow-home.png`
- 视口：桌面 1440px 宽（1200 - 1400px）；窄屏 390px 宽（1400px）
- 状态：空论坛；填了一半的发帖表单；一条帖子的列表；两条不同分类的列表；帖子详情（含 1 条回复、删除入口）；窄屏论坛页；窄屏首页

### Findings

- [P2] 窄屏下「论坛」按钮被挤到单独一行
  Location: `src/styles.css` -> `@media (max-width: 720px)`
  Evidence: 顶栏在 390px 宽时，搜索框占据剩余宽度，导致「论坛」换行后孤零零占一行，与「发布活动」不在同一行。
  Impact: 两个同级入口被拆散，顶栏显得松散。
  Fix: 窄屏下让搜索框独占一行（`.header-actions .search { flex: 1 1 100%; }`），两个按钮并排落在下一行。已修复并重新截图确认，页面无横向溢出。

### 复核证据（Edge CDP 实测）

- 顶栏按钮顺序实测为「发布活动 | 论坛」，且用 `compareDocumentPosition` 确认论坛按钮位于发布活动之后。
- 空论坛显示「论坛里还没有帖子」，分类计数为「全部 0 / 讨论安排 0 / 提建议 0 / 提问求助 1」等实时数字。
- 发帖：选类型「提问求助」→ 填标题正文 → 提交后跳转 `#/forum/t/topic-…`，详情页类型标识、标题、楼主、正文都正确。
- 回复：在帖子内发表回复后计数变为「1 条」，回复者昵称与内容正确；刷新页面后帖子与回复都还在。
- 发帖昵称：用「小美」发第二帖，详情页显示「楼主 · 小美」，说明发帖人昵称生效（与评论共用同一份昵称记忆）。
- 排序：两条帖子按发帖时间倒序；对较早的帖子回复后，它自动排到列表最前，回复数显示「2 条回复」。
- 分类筛选：点「提建议」显示「这个分类下还没有帖子」，点「提问求助」显示对应帖子。
- 持久化：`localStorage` 的 `campus-activity-hub:forum:v1` 中帖子数与回复数正确。
- 窄屏 390px：顶栏、论坛页、首页均无横向溢出。
- 全程控制台无报错、无警告。


## 数据准确性说明

- 09 号「程序设计训练营补充通知」并入 01 号「蓝桥杯程序设计校内训练营」，作为「最新通知」展示。
- 20 号「创新创业项目团队补充说明」并入 03 号「大学生创新创业项目团队招募」，作为「最新通知」展示。
- 24 号「校园兼职福利分享」与 25 号「数码新品体验交流」未收录进页面，保留在 `INFO_PENDING_REVIEW` 并注明原因（导流特征明显、缺少主办方与地点）。
- 原始信息未提供的字段统一标注「信息未注明」，没有补充任何推测内容。
- 用户自己发布的内容由发布者负责，页面不代为核实，只标注「学生自发」并提示其他同学自行确认。

## final result

blocked

阻塞原因：缺少源视觉稿（本会话图像生成工具不可用，未生成候选视觉方案），无法完成源稿与实现的并排保真度比对。

第三、四轮（本地持久化 / 发布活动 / 评论 / 校园论坛）的复核结论：功能侧与视觉侧自检均已完成。走查覆盖「评论 → 刷新 → 发布 → 刷新 → 重新打开链接 → 站内搜索 → 编辑 → 删除」全流程，数据在刷新与重开后保留，控制台无报错；`npm run build` 与 `npm run test:sites` 通过。本轮发现并修复 P1 一项（发布后的内容缺少找回入口），无遗留 P0/P1/P2 问题。仍未解决的只有「无源稿可比对」这一项，与本轮改动无关。
